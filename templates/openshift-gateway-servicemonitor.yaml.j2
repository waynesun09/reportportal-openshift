apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    name: gateway
  name: gateway-metrics
  namespace: {{ namespace }}
spec:
  endpoints:
    - port: metrics
  selector:
    matchLabels:
      component: gateway
