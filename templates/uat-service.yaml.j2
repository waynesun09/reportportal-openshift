apiVersion: v1
kind: Service
metadata:
  annotations:
    infoEndpoint: /info
    service: uat
  labels:
    app: reportportal
  name: uat
  namespace: '{{ namespace }}'
spec:
  ports:
  - name: headless
    port: 9999
    protocol: TCP
    targetPort: 9999
  selector:
    component: uat
  type: ClusterIP
